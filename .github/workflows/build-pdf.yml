name: Build Vivliostyle PDF

on:
  push:
    branches:
      - main

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: yarn install

      - name: Build PDF (Color)
        run: yarn build  # ← build:press から build に変更

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: ebook-pdf
          path: book/output/ebook.pdf  # ← press.pdf から ebook.pdf に変更